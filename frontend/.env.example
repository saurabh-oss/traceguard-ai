# Leave empty for local dev (Vite proxy handles it)
# Set to your Railway/backend URL for production builds
VITE_API_URL=
# Must match API_KEY set in Railway backend variables (leave empty if auth is disabled)
VITE_API_KEY=
